## Quantitative Investment Model

**정량 투자 학습과 전략 아이디어 테스트를 위한 올인원 Python/웹 투자 모델**입니다.  
히스토리컬 가격과 팩터 데이터를 자동으로 수집하고, 수익·위험 지표와 팩터 노출도를 한 번에 보여주는 **교육용 퀀트 리서치 툴**입니다.

> ⚠️ **면책 조항**  
> 이 도구는 **교육 및 리서치용**으로만 제공됩니다.  
> 실제 투자 자문 또는 매매 신호가 아니며, 이를 기반으로 한 모든 투자 의사결정은 사용자 본인의 책임입니다.

---

## 이 도구가 제공하는 가치

- **기초부터 실전까지 한 번에**: 단순 수익률 계산을 넘어, **변동성·샤프 비율·최대 낙폭·VaR·Fama‑French 팩터 모델**까지 한 앱에서 경험할 수 있습니다.
- **현실 요인 반영(선택)**: **거래비용·슬리피지·스프레드(미시구조)·세금**을 옵션으로 넣으면, 세후·비용 반영 수익률과 샤프 비율을 함께 계산해 더 현실에 가까운 비교가 가능합니다.
- **완전 자동 데이터 수집**: 티커와 기간만 입력하면 `yfinance`와 Ken French Data Library에서 데이터를 자동으로 가져옵니다.
- **웹 앱 + CLI 동시 지원**:  
  - 웹 브라우저에서 직관적인 UI로 사용  
  - 터미널에서 스크립트처럼 빠르게 반복 실험
- **양방향 학습 도구**: 정량 금융 이론(수익률, 리스크, VaR, 팩터 모델)을 **실제 코드와 결과**로 확인하며 학습할 수 있습니다.
- **확장성 높은 구조**: 함수 단위로 잘 분리되어 있어, 나만의 전략·팩터·포트폴리오 로직을 쉽게 추가할 수 있습니다.

---

## 핵심 기능 하이라이트

### 1. 데이터 수집 및 전처리

- **yfinance** 기반 자동 가격 수집
  - **입력**: 티커 리스트, 시작일, 종료일  
  - **출력**: 날짜 인덱스를 가진 **조정 종가(Adjusted Close)** 데이터프레임
- 티커별로 데이터를 개별 다운받아 **구조가 깨지지 않도록 안전하게 병합**
- 결측치가 과도한 티커는 자동으로 제거하여 **신뢰도 있는 분석만 수행**

### 2. 수익률·리스크 분석

- **일별 수익률 계산**: \( r_t = \frac{P_t}{P_{t-1}} - 1 \)
- **누적 수익률**: \( (1 + r_t) \)를 기간 전체 곱해 성장 곡선 도출
- **연율화 수익률**: 거래일 수(252일)를 기준으로 성장률을 연간 기준으로 변환
- **연율화 변동성**: 일별 수익률 표준편차 × \( \sqrt{252} \)
- **샤프 비율**:  
  \[
  \text{Sharpe} = \frac{\text{연율화 수익률} - \text{무위험 수익률}}{\text{연율화 변동성}}
  \]
- **최대 낙폭(Max Drawdown)**: 피크 대비 최대 골(peak‑to‑trough) 하락률 계산

이 모든 지표를 **티커별 테이블**로 한 눈에 비교할 수 있습니다.

### 2-1. 비용·세금·미시구조 반영 (선택)

- **거래비용(transaction cost)**: 편도 비용(%)을 입력하면 진입·청산 시 각 1회씩 반영합니다.
- **슬리피지(slippage)**: 체결가와 기대가 차이를 편도 %로 반영합니다.
- **스프레드/미시구조(spread)**: bid-ask 스프레드 등 편도 비용을 %로 넣을 수 있습니다.
- **세금(tax)**: 양의 수익에 대해 지정한 세율을 적용한 **세후 누적·연율화 수익률**과 **세후 샤프 비율**을 계산합니다.

비용·세금을 넣으면 테이블에 **누적 수익률(비용·세금 반영)**, **연율화 수익률(비용·세금 반영)**, **샤프 비율(비용·세금 반영)**, **최대 낙폭(비용 반영)** 컬럼이 추가됩니다. 기본값은 모두 0이라, 넣지 않으면 기존과 동일하게 **비반영** 결과만 나옵니다.

### 3. 파라메트릭 VaR (Value‑at‑Risk)

- 사용자가 선택한 신뢰수준(예: 90% / 95% / 99%)에서  
  **정규분포 가정** 하의 일별 VaR(최대 예상 손실)을 계산합니다.
- 포트폴리오/자산의 **하방 리스크를 숫자로 체감**할 수 있어, 리스크 관리 개념 학습에 최적화되어 있습니다.

### 4. Fama‑French 3‑팩터 모델

- Ken French Data Library에서 **일별 3‑팩터(Mkt‑RF, SMB, HML, RF)** 자동 다운로드
- 각 자산에 대해 다음 회귀식을 적합:
  \[
  R_i - R_f = \\alpha + \\beta_{Mkt}(Mkt-RF) + \\beta_{SMB}SMB + \\beta_{HML}HML + \\varepsilon
  \]
- 산출 값:
  - **α (알파)**: 팩터로 설명되지 않는 초과 수익
  - **β 계수들**: 시장, 사이즈, 가치 팩터에 대한 민감도
- `statsmodels` OLS 회귀 결과를 **전문 리포트 형태(summary)** 로 출력
- 팩터별 β를 **막대 그래프**로 시각화하여,  
  “이 종목/포트폴리오가 어떤 팩터에 얼마나 노출되어 있는지” 직관적으로 파악 가능

### 5. 웹 기반 대시보드

- **Streamlit** 기반 모던 웹 UI
  - 사이드바에서 티커, 날짜, 무위험 수익률, **비용·세금·미시구조(선택)**, VaR 옵션, 팩터 모델 옵션을 직관적으로 설정
  - 메인 화면에서:
    - 수익률·리스크 요약 테이블
    - VaR 테이블
    - 가격 시계열(기준일=100), 누적 수익률
    - 팩터 β 막대 그래프, 샤프 비율 차트
    - 티커별 Fama‑French 회귀 요약 (접이식 상세보기)
- 별도의 자바스크립트/프론트엔드 없이 **순수 Python 코드만으로 웹 리포트 구현**

### 6. CLI(커맨드라인) 분석 모드

- 연구·자동화 스크립트 작성에 유용한 **CLI 버전** 제공:
  - `input()` 기반으로 티커/기간/무위험 수익률/옵션 입력
  - 동일한 계산 로직을 터미널 환경에서 그대로 실행
- 백테스트 스크립트나 배치 리포트에 쉽게 통합 가능

---

## 설치 및 실행 방법

### 1) 설치

```bash
pip install -r requirements.txt
```

또는 개별 설치:

```bash
pip install pandas numpy matplotlib yfinance statsmodels streamlit
```

### 2) 웹 앱 실행 (권장)

```bash
streamlit run app.py
```

또는 실행 스크립트 사용:

```bash
chmod +x run_app.sh
./run_app.sh
```

브라우저에서 `http://localhost:8501` 로 접속합니다.

### 3) CLI 실행

```bash
python quant_investing_model.py
```

---

## 예시 워크플로우

1. **티커 입력**: `AAPL, MSFT, SPY`
2. **기간 설정**: `2020-01-01` ~ `2024-12-31`
3. **무위험 수익률**: `0.03` (3%)
4. **옵션 선택**:
   - 비용·세금 반영: 거래비용 0.1%, 슬리피지 0.05%, 세율 25% 등 (선택)
   - VaR 계산: ON, 신뢰수준 95%
   - Fama‑French 3‑팩터 분석: ON
5. **결과 해석**:
   - 어떤 자산이 높은 **위험 대비 보상(샤프 비율)** 을 갖는지 비교
   - **최대 낙폭**을 통해 하락장 민감도 파악
   - **팩터 β**를 통해 “이 포트폴리오는 성장/가치/소형주 어느 쪽에 더 베팅되어 있는가?” 분석

---

## 활용 아이디어

- **정량 투자 입문 학습용**: 교과서에서 보는 공식들을 실제 시장 데이터에 적용해보며 이해
- **전략 아이디어 프로토타이핑**: 간단한 룰을 추가해 팩터 기반 전략의 감을 잡는 용도
- **리스크/팩터 리포트 템플릿**: 실제 운용 리포트의 축소판으로 활용 가능
- **교육·워크숍 데모**: 수업/세미나에서 정량 분석 과정을 실시간으로 시연

---

## 한계와 주의사항

- **비용·세금·미시구조**: 거래비용·슬리피지·스프레드·세금은 **옵션으로 입력 시** 반영됩니다. 기본값은 0이며, 반영 시에는 편도 비용 합산 후 진입/청산 각 1회, 세금은 양의 수익에 대한 단일 세율의 간이 모형입니다. 실시간 유동성·시장충격의 정교한 미시구조 모형은 포함되지 않습니다.
- VaR 및 팩터 모델은 **역사적 데이터와 단순 가정(정규분포, 선형 관계 등)** 에 기반합니다.
- 과거 성과가 미래 수익을 보장하지 않으며,  
  이 도구의 출력은 **투자 손실에 대한 어떠한 보증도 제공하지 않습니다.**

이 프로젝트는 “**정량 투자 사고방식과 도구를 익히기 위한 출발점**”으로 설계되었습니다.  
비용·세금을 켜서 현실에 가까운 비교를 해보고, 필요에 따라 팩터를 추가하거나 포트폴리오 구성/최적화, 백테스트 로직 등을 얹어 **본인만의 퀀트 리서치 환경**으로 발전시키실 수 있습니다.

